const ImageUpload = ({ store, date, productName, barcode }) => {
  return (
    <div>
      <h3>이미지 업로드</h3>
      {/* 여기에 이미지 업로드 관련 로직 추가 */}
    </div>
  );
};

export default ImageUpload;  // ✅ default export 추가
